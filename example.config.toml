# Mirage configuration example
#
# To configure Mirage create a copy of that file named config.toml

[log]
# The default log level
# Possible values are: trace, debug, info, warn, error, off
level = "info"
