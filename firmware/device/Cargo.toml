[package]
name = "device"
version = "0.1.0"
edition = "2024"

license = "MIT"

[[bin]]
name = "device"
path = "main.rs"

[dependencies]
miralis_abi = { path = "../../crates/abi" }
log = { workspace = true }
